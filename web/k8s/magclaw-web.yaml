apiVersion: v1
kind: ConfigMap
metadata:
  name: magclaw-web-config
data:
  server.yaml: |
    server:
      host: "0.0.0.0"
      port: 6543
      public_url: "https://magclaw.example.com"
      deployment: "cloud"
      data_dir: "/var/lib/magclaw"
      require_postgres: true
    auth:
      allow_signups: true
      require_login: true
    database:
      postgres_url: "postgres://magclaw:change-me@postgres:5432/magclaw"
      name: "magclaw_server"
      schema: "magclaw"
      maintenance_database: "postgres"
      create: false
    storage:
      attachment_storage: "pvc"
      upload_dir: ""
      local_file_storage_fallback: true
      write_state_json: false
    email:
      smtp_host: "smtp.example.com"
      smtp_port: 465
      smtp_tls: false
      from_address: "noreply@example.com"
      from_name: "MagClaw"
---
apiVersion: v1
kind: Secret
metadata:
  name: magclaw-web-secret
type: Opaque
stringData:
  MAGCLAW_SESSION_SECRET: "change-me"
  MAGCLAW_MAIL_FROM: "MagClaw <noreply@example.com>"
  MAGCLAW_SMTP_USER: "change-me"
  MAGCLAW_SMTP_PASSWORD: "change-me"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: magclaw-uploads
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: magclaw-web
  labels:
    app: magclaw-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: magclaw-web
  template:
    metadata:
      labels:
        app: magclaw-web
    spec:
      containers:
        - name: magclaw-web
          image: ghcr.io/your-org/magclaw-web:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 6543
          envFrom:
            - secretRef:
                name: magclaw-web-secret
          env:
            - name: MAGCLAW_CONFIG_FILE
              value: /etc/magclaw/server.yaml
          volumeMounts:
            - name: config
              mountPath: /etc/magclaw
              readOnly: true
            - name: uploads
              mountPath: /var/lib/magclaw/uploads
          livenessProbe:
            httpGet:
              path: /api/healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /api/readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: config
          configMap:
            name: magclaw-web-config
        - name: uploads
          persistentVolumeClaim:
            claimName: magclaw-uploads
---
apiVersion: v1
kind: Service
metadata:
  name: magclaw-web
spec:
  selector:
    app: magclaw-web
  ports:
    - name: http
      port: 80
      targetPort: http
