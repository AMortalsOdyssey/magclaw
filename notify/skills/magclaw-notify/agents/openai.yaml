interface:
  display_name: "MagClaw Notify"
  short_description: "Send a clear, explicitly authorized structured work summary."
  default_prompt: "Use $magclaw-notify to send a concise structured summary of this turn to the exact group I named."
policy:
  allow_implicit_invocation: false
